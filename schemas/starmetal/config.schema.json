{
  "$defs": {
    "AdminConfig": {
      "properties": {
        "enabled": {
          "default": false,
          "type": "boolean"
        },
        "tokens": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "AuthConfig": {
      "properties": {
        "enabled": {
          "default": false,
          "type": "boolean"
        },
        "tokens": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "Ecosystem": {
      "description": "Supported package ecosystems.",
      "enum": ["pypi", "npm", "cargo", "hex", "maven", "rubygems", "nuget", "pub"],
      "type": "string"
    },
    "EncryptionConfig": {
      "properties": {
        "enabled": {
          "default": false,
          "type": "boolean"
        },
        "key_file": {
          "type": ["string", "null"]
        }
      },
      "type": "object"
    },
    "GcsConfig": {
      "properties": {
        "bucket": {
          "type": "string"
        },
        "credential_path": {
          "type": ["string", "null"]
        },
        "endpoint": {
          "type": ["string", "null"]
        }
      },
      "required": ["bucket"],
      "type": "object"
    },
    "MetadataConfig": {
      "description": "Optional Postgres-backed content model (ADR-0020). When enabled, publishes dual-write the\ncomponent -> asset -> blob content model, giving cross-ecosystem blob dedup, content-address\nintegrity, and reference-counted garbage collection alongside the flat object store.",
      "properties": {
        "apply_schema": {
          "default": true,
          "description": "Apply the content-model schema on startup — convenient for turnkey deployments. Disable when\nmigrations are managed out of band.",
          "type": "boolean"
        },
        "database_url": {
          "description": "Postgres connection URL (`postgresql://user:password@host:port/database`). Required when\n`enabled` is true; redacted from the admin API's config view.",
          "type": ["string", "null"]
        },
        "enabled": {
          "default": false,
          "type": "boolean"
        },
        "gc_grace_secs": {
          "default": 86400,
          "description": "Grace window in seconds applied to every blob newly soft-deleted by a GC sweep before a\nlater sweep's compact step may hard-delete it.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "gc_interval_secs": {
          "default": 0,
          "description": "Interval in seconds between scheduled garbage-collection sweeps (ADR-0020 Stage 2d). `0`\n(the default) disables the scheduler. Only effective when `enabled`.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "retention": {
          "$ref": "#/$defs/RetentionPolicy",
          "default": {
            "rules": []
          },
          "description": "The retention policy applied by the scheduled retention sweep when neither a per-repository\nnor a per-ecosystem policy matches a component family. An empty policy (the default) is a\nno-op. Precedence is `retention_per_repository` > `retention_per_ecosystem` > `retention`."
        },
        "retention_interval_secs": {
          "default": 0,
          "description": "Interval in seconds between scheduled retention sweeps (ADR-0020 Stage 2c). `0` (the\ndefault) disables the scheduler. Only effective when `enabled`.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "retention_per_ecosystem": {
          "additionalProperties": {
            "$ref": "#/$defs/RetentionPolicy"
          },
          "default": {},
          "description": "Retention policies keyed by canonical ecosystem name (`\"pypi\"`, `\"npm\"`, `\"cargo\"`, `\"hex\"`,\n`\"maven\"`, `\"rubygems\"`, `\"nuget\"`, `\"pub\"`). A family whose ecosystem matches uses this\npolicy in preference to the global `retention` (but a matching `retention_per_repository`\nstill wins). Empty by default.",
          "type": "object"
        },
        "retention_per_repository": {
          "additionalProperties": {
            "$ref": "#/$defs/RetentionPolicy"
          },
          "default": {},
          "description": "Retention policies keyed by repository attribution string (see\n[`crate::content::Component::repository`]). A family whose repository matches uses this\npolicy in preference to both `retention_per_ecosystem` and the global `retention`. Keys are\nfree-form (no validation). Empty by default.",
          "type": "object"
        }
      },
      "type": "object"
    },
    "OidcConfig": {
      "description": "Static-JWKS OIDC bearer-token identity backend (ADR-0022).\n\nA second [`Authenticator`](crate::authz::Authenticator) backend, composed ahead of the flat-token\n`LocalAuthorizer`: when `enabled`, a compact JWS bearer is verified against a **static** JWKS\nsupplied inline ([`jwks`](OidcConfig::jwks)) or from a file\n([`jwks_file`](OidcConfig::jwks_file)). This is deliberately offline — there is no JWKS-URL fetch,\nOIDC discovery, or token refresh; a live IdP integration is a later stage. Off by default, so a\ndeployment that omits this section authenticates exactly as before.\n\nThe validator (in `starmetal-oidc`) verifies the signature (RS256 or ES256 only — `alg: none` and\nany other algorithm are rejected), the `exp` claim (with [`leeway_secs`](OidcConfig::leeway_secs)),\nthe `iss` claim (must equal [`issuer`](OidcConfig::issuer)), and the `aud` claim (must contain\n[`audience`](OidcConfig::audience)), then maps [`principal_claim`](OidcConfig::principal_claim) to a\n`Principal::User`.",
      "properties": {
        "audience": {
          "default": "",
          "description": "The expected audience. A token whose `aud` claim does not contain this value is rejected.\nRequired (non-empty) when `enabled`.",
          "type": "string"
        },
        "enabled": {
          "default": false,
          "description": "Whether the OIDC identity backend is active. When `false` (the default) no JWT validation is\nwired and authentication is unchanged.",
          "type": "boolean"
        },
        "issuer": {
          "default": "",
          "description": "The expected token issuer. A token whose `iss` claim does not equal this exactly is rejected.\nRequired (non-empty) when `enabled`.",
          "type": "string"
        },
        "jwks": {
          "description": "Inline JWKS document (RFC 7517 JSON), e.g. `{ \"keys\": [ ... ] }`. Takes precedence over\n[`jwks_file`](OidcConfig::jwks_file) when both are set. The keys hold only public material.",
          "type": ["string", "null"]
        },
        "jwks_file": {
          "description": "Path to a file containing the JWKS document, read once at startup. Used when\n[`jwks`](OidcConfig::jwks) is unset.",
          "type": ["string", "null"]
        },
        "leeway_secs": {
          "default": 60,
          "description": "Clock-skew leeway, in seconds, applied when checking `exp`. Defaults to 60.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "principal_claim": {
          "default": "sub",
          "description": "The claim mapped to the authenticated principal's id. Defaults to `sub`.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "PolicyConfig": {
      "properties": {
        "allowed_licenses": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "block_unlicensed": {
          "default": false,
          "type": "boolean"
        },
        "blocked_packages": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "max_vuln_severity": {
          "$ref": "#/$defs/VulnSeverity",
          "default": "critical"
        }
      },
      "type": "object"
    },
    "PublishMode": {
      "enum": ["local", "local-and-forward", "forward-only"],
      "type": "string"
    },
    "PublishTokenConfig": {
      "properties": {
        "ecosystems": {
          "default": [],
          "items": {
            "$ref": "#/$defs/Ecosystem"
          },
          "type": "array"
        },
        "packages": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "scopes": {
          "default": [],
          "items": {
            "$ref": "#/$defs/TokenScope"
          },
          "type": "array"
        },
        "token": {
          "type": "string"
        }
      },
      "required": ["token"],
      "type": "object"
    },
    "PublishingConfig": {
      "properties": {
        "allow_overwrite": {
          "default": false,
          "type": "boolean"
        },
        "allow_shadowing": {
          "default": false,
          "type": "boolean"
        },
        "enabled": {
          "default": false,
          "type": "boolean"
        },
        "mode": {
          "$ref": "#/$defs/PublishMode",
          "default": "local"
        },
        "tokens": {
          "default": [],
          "items": {
            "$ref": "#/$defs/PublishTokenConfig"
          },
          "type": "array"
        },
        "upstream": {
          "additionalProperties": {
            "$ref": "#/$defs/PublishingUpstreamConfig"
          },
          "default": {},
          "type": "object"
        }
      },
      "type": "object"
    },
    "PublishingUpstreamConfig": {
      "properties": {
        "enabled": {
          "default": false,
          "type": "boolean"
        },
        "password_env": {
          "type": ["string", "null"]
        },
        "token_env": {
          "type": ["string", "null"]
        },
        "username_env": {
          "type": ["string", "null"]
        }
      },
      "type": "object"
    },
    "QuotaConfig": {
      "description": "Publish quota controls (ADR-0021). When enabled, a hosted publish that would push its\n`(ecosystem, namespace)` coordinate over its resolved [`QuotaLimits`] is denied with\n[`crate::supply_chain::PolicyReason::QuotaExceeded`]. The namespace is the component's grouping\n(npm scope, Maven group id; see [`crate::package::PackageName::publish_namespace`]) — `None` for\necosystems without one. Off by default so it is inert until an operator configures a limit; the\nreserve/reconcile ledger itself is process-local and lives in `starmetal-service`.",
      "properties": {
        "default_limits": {
          "anyOf": [
            {
              "$ref": "#/$defs/QuotaLimits"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Fallback limits applied to any ecosystem without a `per_ecosystem` entry. `None` leaves an\nunlisted ecosystem unlimited."
        },
        "enabled": {
          "default": false,
          "description": "Enable publish quota enforcement. Off by default.",
          "type": "boolean"
        },
        "per_ecosystem": {
          "additionalProperties": {
            "$ref": "#/$defs/QuotaLimits"
          },
          "default": {},
          "description": "Quota limits for a specific ecosystem, keyed by its config name (`\"pypi\"`, `\"npm\"`, `\"cargo\"`,\n`\"hex\"`, `\"maven\"`, `\"rubygems\"`, `\"nuget\"`, `\"pub\"`). Takes precedence over `default_limits`\nfor a matching ecosystem.",
          "type": "object"
        }
      },
      "type": "object"
    },
    "QuotaLimits": {
      "description": "One quota ceiling pair (ADR-0021) for a `(ecosystem, namespace)` coordinate. Each dimension is\nindependently optional; `None` leaves that dimension unlimited.",
      "properties": {
        "max_bytes": {
          "default": null,
          "description": "Maximum cumulative artifact bytes for the coordinate. `None` is unlimited.",
          "format": "uint64",
          "minimum": 0,
          "type": ["integer", "null"]
        },
        "max_versions": {
          "default": null,
          "description": "Maximum number of published versions for the coordinate. `None` is unlimited.",
          "format": "uint64",
          "minimum": 0,
          "type": ["integer", "null"]
        }
      },
      "type": "object"
    },
    "RepositoryConfig": {
      "description": "A declared repository (ADR-0019): a `(kind, ecosystem)` surface mounted under\nits own URL segment.\n\nWhen [`Config::repositories`] is empty, Starmetal derives one `proxy`\nrepository per enabled `[upstream]` ecosystem (see\n[`Config::resolved_repositories`]), preserving the historical proxy-only\nbehavior. `proxy` and `group` repositories are supported; `hosted` is\nreserved for a later stage and rejected by [`Config::validate_mvp`].",
      "properties": {
        "ecosystem": {
          "$ref": "#/$defs/Ecosystem",
          "description": "The ecosystem this repository serves."
        },
        "kind": {
          "$ref": "#/$defs/RepositoryKind",
          "description": "The repository kind: `proxy`, `hosted`, or `group`."
        },
        "members": {
          "default": [],
          "description": "Member repository names for a `group` (ADR-0019). Each must name another declared repository\nof the same ecosystem. Empty (and required to be empty) for `proxy` and `hosted` kinds; a\n`group` requires at least one member. Members resolve in order — first-match for artifacts,\nmerge-all for version listings.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "name": {
          "description": "URL mount segment and identity of the repository (e.g. `pypi`). Must be\nunique across repositories.",
          "type": "string"
        }
      },
      "required": ["name", "kind", "ecosystem"],
      "type": "object"
    },
    "RepositoryKind": {
      "description": "The kind of a repository, per ADR-0018/ADR-0019.\n\nThis is a typed distinction rather than a boolean: a repository is exactly one\nof proxy, hosted, or group. The value is config-facing and serializes as a\nlowercase string (`\"proxy\"`, `\"hosted\"`, `\"group\"`).",
      "oneOf": [
        {
          "const": "proxy",
          "description": "Caches an upstream registry (the pull-through behavior that exists today).",
          "type": "string"
        },
        {
          "const": "hosted",
          "description": "Stores artifacts published directly to Starmetal.",
          "type": "string"
        },
        {
          "const": "group",
          "description": "Virtual/aggregate repository presenting multiple members behind one URL.",
          "type": "string"
        }
      ]
    },
    "RetentionPolicy": {
      "description": "An ordered set of [`RetentionRule`]s applied together as a retention policy.\n\nThis is a plain configuration container; it holds no evaluation logic.",
      "properties": {
        "rules": {
          "default": [],
          "description": "Rules that make up this policy, evaluated by a later stage.",
          "items": {
            "$ref": "#/$defs/RetentionRule"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "RetentionRule": {
      "description": "A single logical retention rule (ADR-0020).\n\nRetention is decoupled from garbage collection: these rules decide which component and asset\nrows to *delete*, after which reference-counted GC physically reclaims any blobs that become\nunreferenced. This enum is the typed, config-facing description of a rule only — it carries no\nevaluation engine.\n\nThe serde representation is internally tagged on a `strategy` field, for example:\n\n```json\n{ \"strategy\": \"keep-latest\", \"count\": 10 }\n```",
      "oneOf": [
        {
          "description": "Keep only the N most recent versions of a component.",
          "properties": {
            "count": {
              "description": "Number of latest versions to retain.",
              "format": "uint",
              "minimum": 0,
              "type": "integer"
            },
            "strategy": {
              "const": "keep-latest",
              "type": "string"
            }
          },
          "required": ["strategy", "count"],
          "type": "object"
        },
        {
          "description": "Retain versions downloaded within the trailing window; delete the rest.",
          "properties": {
            "strategy": {
              "const": "last-downloaded",
              "type": "string"
            },
            "within_days": {
              "description": "Retention window, in days, measured from the last download.",
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": ["strategy", "within_days"],
          "type": "object"
        },
        {
          "description": "Retain versions updated within the trailing window; delete the rest.",
          "properties": {
            "strategy": {
              "const": "last-updated",
              "type": "string"
            },
            "within_days": {
              "description": "Retention window, in days, measured from the last update.",
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": ["strategy", "within_days"],
          "type": "object"
        },
        {
          "description": "Retain versions whose version string matches the given regular expression.",
          "properties": {
            "pattern": {
              "description": "Regular expression evaluated against the version string.",
              "type": "string"
            },
            "strategy": {
              "const": "regex",
              "type": "string"
            }
          },
          "required": ["strategy", "pattern"],
          "type": "object"
        },
        {
          "description": "Select versions by their prerelease status.",
          "properties": {
            "prerelease": {
              "description": "When `true`, the rule targets prerelease versions; when `false`, stable versions.",
              "type": "boolean"
            },
            "strategy": {
              "const": "is-prerelease",
              "type": "string"
            }
          },
          "required": ["strategy", "prerelease"],
          "type": "object"
        }
      ]
    },
    "S3Config": {
      "properties": {
        "bucket": {
          "type": "string"
        },
        "endpoint": {
          "type": ["string", "null"]
        },
        "region": {
          "type": "string"
        }
      },
      "required": ["bucket", "region"],
      "type": "object"
    },
    "SbomConfig": {
      "description": "SBOM generation controls (ADR-0024). Independent of the scanner: SBOMs are generated from the\npublish request, so this needs no scanner feature or backend.",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Generate and store SBOM documents on publish. Off by default.",
          "type": "boolean"
        },
        "formats": {
          "default": ["cyclonedx", "spdx"],
          "description": "Formats to generate for each artifact. Defaults to both CycloneDX and SPDX.",
          "items": {
            "$ref": "#/$defs/SbomFormat"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "SbomFormat": {
      "description": "Standard SBOM document formats Starmetal generates and stores.",
      "oneOf": [
        {
          "const": "cyclonedx",
          "description": "CycloneDX (<https://cyclonedx.org>).",
          "type": "string"
        },
        {
          "const": "spdx",
          "description": "SPDX (<https://spdx.dev>).",
          "type": "string"
        }
      ]
    },
    "ScannerKind": {
      "description": "Which artifact scanner backs the supply-chain vulnerability gate (ADR-0024).",
      "oneOf": [
        {
          "const": "osv",
          "description": "The OSV.dev query-API scanner.",
          "type": "string"
        }
      ]
    },
    "ServerConfig": {
      "properties": {
        "bind": {
          "default": "127.0.0.1:8080",
          "type": "string"
        },
        "cors_allowed_origins": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "max_upload_bytes": {
          "default": 536870912,
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "public_base_url": {
          "default": null,
          "type": ["string", "null"]
        }
      },
      "type": "object"
    },
    "SigningAlgorithm": {
      "enum": ["ed25519", "ecdsa-p256-sha256", "ml-dsa65"],
      "type": "string"
    },
    "SigningConfig": {
      "properties": {
        "enabled": {
          "default": false,
          "type": "boolean"
        },
        "keys": {
          "default": [],
          "items": {
            "$ref": "#/$defs/SigningKeyConfig"
          },
          "type": "array"
        },
        "mode": {
          "$ref": "#/$defs/SigningMode",
          "default": "sign-and-verify"
        },
        "sign_cached_upstream": {
          "default": false,
          "type": "boolean"
        },
        "trust_roots": {
          "default": [],
          "items": {
            "$ref": "#/$defs/SigningTrustRootConfig"
          },
          "type": "array"
        },
        "verify_on_read": {
          "default": false,
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "SigningKeyConfig": {
      "properties": {
        "algorithm": {
          "$ref": "#/$defs/SigningAlgorithm"
        },
        "certificate_chain_file": {
          "type": ["string", "null"]
        },
        "certificate_file": {
          "type": ["string", "null"]
        },
        "ecosystems": {
          "default": [],
          "items": {
            "$ref": "#/$defs/Ecosystem"
          },
          "type": "array"
        },
        "id": {
          "type": "string"
        },
        "packages": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "private_key_file": {
          "type": ["string", "null"]
        },
        "private_key_password_env": {
          "type": ["string", "null"]
        },
        "public_key_file": {
          "type": ["string", "null"]
        },
        "status": {
          "$ref": "#/$defs/SigningKeyStatus",
          "default": "active"
        }
      },
      "required": ["id", "algorithm"],
      "type": "object"
    },
    "SigningKeyStatus": {
      "enum": ["active", "verify-only", "disabled"],
      "type": "string"
    },
    "SigningMode": {
      "enum": ["sign-only", "sign-and-verify", "verify-only"],
      "type": "string"
    },
    "SigningTrustRootConfig": {
      "properties": {
        "allowed_algorithms": {
          "default": [],
          "items": {
            "$ref": "#/$defs/SigningAlgorithm"
          },
          "type": "array"
        },
        "certificate_file": {
          "type": "string"
        },
        "id": {
          "type": "string"
        }
      },
      "required": ["id", "certificate_file"],
      "type": "object"
    },
    "StorageConfig": {
      "properties": {
        "backend": {
          "default": "fs",
          "type": "string"
        },
        "gcs": {
          "anyOf": [
            {
              "$ref": "#/$defs/GcsConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "options": {
          "additionalProperties": {
            "type": "string"
          },
          "default": {},
          "type": "object"
        },
        "path": {
          "default": null,
          "type": ["string", "null"]
        },
        "s3": {
          "anyOf": [
            {
              "$ref": "#/$defs/S3Config"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        }
      },
      "type": "object"
    },
    "SupplyChainConfig": {
      "description": "Optional supply-chain security controls (ADR-0024). When enabled, publishes are gated at ingest:\neach artifact is scanned and denied when a finding exceeds `policies.max_vuln_severity`. Requires\nthe corresponding scanner build feature (e.g. `scanner-osv`); disabled by default.",
      "properties": {
        "enabled": {
          "default": false,
          "type": "boolean"
        },
        "enforce_on_serve": {
          "default": false,
          "description": "Also enforce the vulnerability gate at serve: `get_artifact` consults each artifact's stored\nscan report (scanning on demand and caching it when absent) and refuses to serve a finding\nthat exceeds `policies.max_vuln_severity`. Off by default — ingest gating only.",
          "type": "boolean"
        },
        "ingest_quarantine": {
          "default": false,
          "description": "Hold a blocked hosted *publish* as a recoverable quarantine record instead of hard-denying it:\nthe uploaded bytes are parked off the live path for operator review, then promoted (which\ncompletes the deferred publish) or rejected (which purges the bytes) via the admin API. Off by\ndefault — a blocked publish is denied. Only effective with a scanner attached.",
          "type": "boolean"
        },
        "osv_endpoint": {
          "default": null,
          "description": "Override the OSV endpoint base URL (defaults to the public `https://api.osv.dev`). Useful for\na self-hosted OSV mirror.",
          "type": ["string", "null"]
        },
        "quarantine": {
          "default": false,
          "description": "Hold a serve-time gate block as a recoverable quarantine record (an operator can promote or\nreject it via the admin API) instead of a terminal deny. Off by default — blocks are hard\ndenials. Only effective with `enforce_on_serve` and a scanner attached.",
          "type": "boolean"
        },
        "quota": {
          "$ref": "#/$defs/QuotaConfig",
          "default": {
            "default_limits": null,
            "enabled": false,
            "per_ecosystem": {}
          },
          "description": "Publish quota reserve/reconcile controls (ADR-0021): a ceiling on published version count\nand/or cumulative artifact bytes per `(ecosystem, namespace)` coordinate, enforced by an\nin-memory ledger around the publish path. Off by default."
        },
        "recorrelation_interval_secs": {
          "default": 0,
          "description": "Interval in seconds between scheduled re-correlation sweeps. Each sweep re-scans every stored\nscan report against the (refreshed) advisory feed and rewrites it, so an artifact that was\nclean when first scanned is re-evaluated as new advisories land (\"scan once, then monitor\").\n`0` (the default) disables the scheduler. Only effective when a scanner is attached.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "require_provenance": {
          "default": false,
          "description": "Require a valid Starmetal in-toto/SLSA provenance attestation to serve/publish an artifact\n(ADR-0024). When set, publishes and cache-fills emit a signed provenance attestation, and a\nmissing or invalid one is denied (`failing-provenance`) at both ingest and serve. Requires\nsigning to be configured. Off by default.",
          "type": "boolean"
        },
        "require_signature": {
          "default": false,
          "description": "Require a valid Starmetal DSSE signature to serve/publish an artifact (ADR-0024). A missing\nor invalid signature is denied (`missing-signature`) at both ingest and serve. Requires\nsigning to be configured (`[signing]`). Off by default.",
          "type": "boolean"
        },
        "sbom": {
          "$ref": "#/$defs/SbomConfig",
          "default": {
            "enabled": false,
            "formats": ["cyclonedx", "spdx"]
          },
          "description": "SBOM generation controls. When enabled, each published artifact gets an SBOM document per\nconfigured format, stored digest-keyed and retrievable via the admin API."
        },
        "scanner": {
          "$ref": "#/$defs/ScannerKind",
          "default": "osv",
          "description": "The scanner backend to use."
        }
      },
      "type": "object"
    },
    "TokenScope": {
      "enum": ["read", "publish", "yank", "admin"],
      "type": "string"
    },
    "UpstreamConfig": {
      "properties": {
        "allow_insecure": {
          "default": false,
          "type": "boolean"
        },
        "allow_private_network": {
          "default": false,
          "type": "boolean"
        },
        "artifact_url": {
          "default": null,
          "type": ["string", "null"]
        },
        "enabled": {
          "default": true,
          "type": "boolean"
        },
        "max_response_bytes": {
          "default": 536870912,
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "url": {
          "type": "string"
        }
      },
      "required": ["url"],
      "type": "object"
    },
    "VulnSeverity": {
      "enum": ["low", "medium", "high", "critical"],
      "type": "string"
    }
  },
  "$id": "https://schemas.starmetal.local/starmetal/config.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Starmetal-owned JSON Schema for starmetal.toml.",
  "properties": {
    "admin": {
      "$ref": "#/$defs/AdminConfig",
      "default": {
        "enabled": false,
        "tokens": []
      }
    },
    "auth": {
      "$ref": "#/$defs/AuthConfig",
      "default": {
        "enabled": false,
        "tokens": []
      }
    },
    "encryption": {
      "$ref": "#/$defs/EncryptionConfig",
      "default": {
        "enabled": false,
        "key_file": null
      }
    },
    "metadata": {
      "$ref": "#/$defs/MetadataConfig",
      "default": {
        "apply_schema": true,
        "database_url": null,
        "enabled": false,
        "gc_grace_secs": 86400,
        "gc_interval_secs": 0,
        "retention": {
          "rules": []
        },
        "retention_interval_secs": 0,
        "retention_per_ecosystem": {},
        "retention_per_repository": {}
      }
    },
    "oidc": {
      "$ref": "#/$defs/OidcConfig",
      "default": {
        "audience": "",
        "enabled": false,
        "issuer": "",
        "leeway_secs": 60,
        "principal_claim": "sub"
      }
    },
    "policies": {
      "$ref": "#/$defs/PolicyConfig",
      "default": {
        "allowed_licenses": [],
        "block_unlicensed": false,
        "blocked_packages": [],
        "max_vuln_severity": "critical"
      }
    },
    "publishing": {
      "$ref": "#/$defs/PublishingConfig",
      "default": {
        "allow_overwrite": false,
        "allow_shadowing": false,
        "enabled": false,
        "mode": "local",
        "tokens": [],
        "upstream": {}
      }
    },
    "repositories": {
      "default": [],
      "items": {
        "$ref": "#/$defs/RepositoryConfig"
      },
      "type": "array"
    },
    "server": {
      "$ref": "#/$defs/ServerConfig",
      "default": {
        "bind": "127.0.0.1:8080",
        "cors_allowed_origins": [],
        "max_upload_bytes": 536870912,
        "public_base_url": null
      }
    },
    "signing": {
      "$ref": "#/$defs/SigningConfig",
      "default": {
        "enabled": false,
        "keys": [],
        "mode": "sign-and-verify",
        "sign_cached_upstream": false,
        "trust_roots": [],
        "verify_on_read": false
      }
    },
    "storage": {
      "$ref": "#/$defs/StorageConfig",
      "default": {
        "backend": "fs",
        "gcs": null,
        "options": {},
        "path": null,
        "s3": null
      }
    },
    "supply_chain": {
      "$ref": "#/$defs/SupplyChainConfig",
      "default": {
        "enabled": false,
        "enforce_on_serve": false,
        "ingest_quarantine": false,
        "osv_endpoint": null,
        "quarantine": false,
        "quota": {
          "default_limits": null,
          "enabled": false,
          "per_ecosystem": {}
        },
        "recorrelation_interval_secs": 0,
        "require_provenance": false,
        "require_signature": false,
        "sbom": {
          "enabled": false,
          "formats": ["cyclonedx", "spdx"]
        },
        "scanner": "osv"
      }
    },
    "upstream": {
      "additionalProperties": {
        "$ref": "#/$defs/UpstreamConfig"
      },
      "default": {
        "cargo": {
          "allow_insecure": false,
          "allow_private_network": false,
          "artifact_url": "https://static.crates.io/crates",
          "enabled": true,
          "max_response_bytes": 536870912,
          "url": "https://index.crates.io"
        },
        "hex": {
          "allow_insecure": false,
          "allow_private_network": false,
          "artifact_url": "https://repo.hex.pm",
          "enabled": true,
          "max_response_bytes": 536870912,
          "url": "https://hex.pm"
        },
        "maven": {
          "allow_insecure": false,
          "allow_private_network": false,
          "artifact_url": null,
          "enabled": true,
          "max_response_bytes": 536870912,
          "url": "https://repo1.maven.org/maven2"
        },
        "npm": {
          "allow_insecure": false,
          "allow_private_network": false,
          "artifact_url": null,
          "enabled": true,
          "max_response_bytes": 536870912,
          "url": "https://registry.npmjs.org"
        },
        "nuget": {
          "allow_insecure": false,
          "allow_private_network": false,
          "artifact_url": null,
          "enabled": true,
          "max_response_bytes": 536870912,
          "url": "https://api.nuget.org/v3/index.json"
        },
        "pub": {
          "allow_insecure": false,
          "allow_private_network": false,
          "artifact_url": null,
          "enabled": true,
          "max_response_bytes": 536870912,
          "url": "https://pub.dev"
        },
        "pypi": {
          "allow_insecure": false,
          "allow_private_network": false,
          "artifact_url": null,
          "enabled": true,
          "max_response_bytes": 536870912,
          "url": "https://pypi.org"
        },
        "rubygems": {
          "allow_insecure": false,
          "allow_private_network": false,
          "artifact_url": "https://rubygems.org",
          "enabled": true,
          "max_response_bytes": 536870912,
          "url": "https://rubygems.org"
        }
      },
      "type": "object"
    }
  },
  "title": "Starmetal Configuration",
  "type": "object",
  "x-starmetal-source": {
    "generated_by": "starmetal-schema-manager",
    "schema_kind": "starmetal-owned-json-schema",
    "source_file": "crates/starmetal-core/src/config.rs",
    "source_ids": ["starmetal-config"],
    "source_kinds": ["starmetal-rust-type"],
    "source_type": "starmetal_core::config::Config",
    "validated_by": "crates/starmetal-core/tests/schema_validation.rs"
  }
}
