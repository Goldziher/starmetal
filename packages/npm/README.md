# StarMetal

StarMetal is a high-performance, self-hosted multi-language package registry and pull-through
registry proxy for private software supply chains.

The npm package installs the `sm` command. On install it downloads the matching prebuilt binary from
GitHub Releases and verifies it against the release checksums file before extraction.

Repository: <https://github.com/Goldziher/starmetal>
